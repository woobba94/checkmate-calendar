{
  "name": "checkmate-calendar",
  "private": true,
  "version": "1.0.0",
  "description": "Checkmate Calendar - Monorepo",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev:landing": "npm run dev --workspace=apps/landing",
    "dev:calendar": "npm run dev --workspace=apps/calendar",
    "build:landing": "npm run build --workspace=apps/landing",
    "build:calendar": "npm run build --workspace=apps/calendar",
    "preview:landing": "npm run preview --workspace=apps/landing",
    "preview:calendar": "npm run preview --workspace=apps/calendar"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}
